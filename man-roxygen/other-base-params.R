#' @param validate_params (`logical(1)`) Should the parameters be validated? It
#'   is not recommended to set this parameter to `FALSE` because if something
#'   fails a non meaningful error is going to be thrown. `TRUE` by default.
#' @param seed (`numeric(1)`) A value to be used as internal seed for
#'   reproducible results. `NULL` by default.
#' @param verbose (`logical(1)`) Should the progress information be printed?
#'   `TRUE` by default.
