#' @param observed (`vector`) The observed values. It has to be coercible to
#'   `<%= coercibleTo %>` and with the same length as `predicted`.
#' @param predicted (`vector`) The predicted values. It has to be coercible to
#'   `<%= coercibleTo %>` and with the same length as `observed`.
#' @param na.rm (`logical(1)`) Should `NA` values be removed?. `TRUE` by
#'   default.
