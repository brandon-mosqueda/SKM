% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kernels.R
\name{kernelize}
\alias{kernelize}
\title{Apply a kernel}
\usage{
kernelize(
  x,
  kernel,
  standardize = TRUE,
  rows_proportion = 0.8,
  arc_cosine_deep = 1,
  gamma = 1/NCOL(x),
  coef0 = 0,
  degree = 3
)
}
\arguments{
\item{x}{(\code{any}) The data to apply the kernel to.}

\item{kernel}{(\code{character(1)}) (case no sensitive) The kernel name to apply.
The available options are \code{"Linear"}, \code{"Polynomial"}, \code{"Exponential"},
\code{"Sigmoid"}, \code{"Gaussian"} and \code{"Arc_cosine"}. Also the \emph{sparse} version of
each kernel can be used as \code{"Sparse_KERNEL"}, where KERNEL is one the
kernels.}

\item{standardize}{(\code{logical(1)}) Should the data be standardized before
applying the kernel? \code{TRUE} by default.}

\item{rows_proportion}{(\code{numeric(1)}) The proportion of rows to be sampled to
compute the kernel. It has to be a value > 0 and <= 1. This parameter is
only  used with \code{"Sparse_"} kernels. 0.8 by default.}

\item{arc_cosine_deep}{(\code{numeric(1)}) The deep to use in the \code{"Arc_cosine"}
and \code{"Sparse_Arc_cosine"} kernels. It has to be a integer value >= 1. 1 by
default.}

\item{gamma}{(\code{numeric}) Parameter needed for all kernels except \code{"Linear"},
\code{"Arc_cosine"} and their \code{"Sparse_"} versions. \code{1 / NCOL(x)} by default.}

\item{coef0}{(\code{numeric}) Parameter needed for \code{"Polynomial"}, \code{"Sigmoid"}
kernels and their \code{"Sparse_"} versions. 0 by default.}

\item{degree}{(\code{numeric}) Parameter needed for \code{"Polynomial"} and
\code{"Sparse_Polynomial"}  kernels. 3 by default.}
}
\value{
A \code{matrix} with the data after applied kernel.
}
\description{
Apply a kernel to your data.
}
\details{
You have to consider that before applying the kernel, the provided data is
converted to matrix with \code{\link[=to_matrix]{to_matrix()}} function, see its documentation for
more details of how categorical variables are handled.
\subsection{kernel}{

The different kernel transformations are described in the following
mathematical expressions:
\itemize{
\item Linear:

\figure{linear_kernel.png}{K(X, Y) = X^T \%*\% Y}
\item Polynomial:

\figure{polynomial_kernel.png}{K(X, Y) = (gamma * X^T \%*\% Y + coef0)^degree}
\item Radial:

\figure{radial_kernel.png}{K(X, Y) = eXp(-gamma * |X - Y|^2)}
\item Sigmoid:

\figure{sigmoid_kernel.png}{K(X, Y) = tanh(gamma * X^T \%*\% Y + coef0)}
}
}
}
\examples{
\dontrun{
kernelize(iris, kernel = "linear")
kernelize(1:10, kernel = "polynomial")
kernelize(
  matrix(rnorm(12), 3, 4),
  kernel = "Sparse_polynomial",
  rows_proportion = 0.5
)
}

}
